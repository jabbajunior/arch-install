When using UEFI for boot, we require a EFI system partition (section 1.9). [Its purpose is to store the UEFI boot loader](https://wiki.archlinux.org/title/EFI_system_partition). 

Also there are multiple command line tools for working with partitions (lsblk and fdisk). I only knew about lsblk.